# macOS
.DS_Store

# Xcode — user-specific state
xcuserdata/
*.xcuserstate
*.xcuserdatad/

# Xcode — build artifacts
build/
DerivedData/
*.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/

# Swift Package Manager
.build/
.swiftpm/
Packages/
Package.resolved

# Local model weights — huge, never commit.
# Rebuild with `cactus download <model>` (see AryaaBluetoothMesh/ios/SETUP.md).
AryaaBluetoothMesh/ios/MeshNode/Models/

# Vendored binary frameworks — rebuild with `cactus build --apple`.
# See AryaaBluetoothMesh/ios/SETUP.md.
AryaaBluetoothMesh/ios/Vendor/

# Python venvs / caches (if any in subfolders)
venv/
.venv/
__pycache__/
*.pyc
