# Transition: Shot 01 → Shot 02 — Scale-Jump to the Scope

**From:** 01 — Cold Open: Zoom Into the Hunt (ends at 0:06 in the final cut)
**To:** 02 — Sniper Overwatch Check-In (starts at 0:06 in the final cut)
**Duration:** 6 seconds of bridging motion
**Inputs for the video generator:** (a) this file, (b) shot 01 hero keyframe image (start frame), (c) shot 02 hero keyframe image (end frame).

## Narrative Purpose
The bridge collapses tactical scale into a single breath: the drone-god overview of the whole dispersed raid on compound `AC-1` narrows, dives, and punches through the sniper `OVER` on his distant ridge outbuilding until the viewer is *inside* his optic. It is the handoff from "here is the map of the mission" to "here is the first voice on the net" — the moment the film zooms from the command tree's silhouette into a single operator's eye.

## Camera Move (start-frame pose → end-frame pose)
- **Start pose (0:00 of the bridge, inherits 01's end frame):** pure bird's-eye, dead-vertical 90° overhead, 24mm-equivalent drone gimbal at ~40 m altitude, compound `AC-1` locked in the center occupying ~70% of the frame, the distant ridge outbuilding with `OVER` visible as a tiny silhouette near the upper-right of the frame ~320 m NNE, all ten operators resolvable with their pale-green callout labels, mesh threads pulsing.
- **0:00 → 0:02 (scale-pivot):** the camera begins a gentle Dutch de-roll from 0°, then breaks its top-down lock — the gimbal tilts forward from 90° overhead to ~60° oblique, yaw whips clockwise to face north-northeast, lens pushes to ~28mm equivalent as the compound slides out of frame bottom-left and the ridge outbuilding swings toward screen center. Altitude holds near 40 m for the first beat to preserve continuity with 01's end pose.
- **0:02 → 0:04 (dive):** the camera accelerates into a low, fast dolly across the landscape toward `OVER`'s hide — altitude drops from ~40 m to ~8 m, tilt passes through 30° down and keeps dropping toward horizontal, lens zooms past 35mm into a 50mm shoulder, motion-blur streaks the olive/poplar tree line and the irrigation ditches as they sweep past the lower frame. Ankle-height dust ribbons scream through the foreground as speed-lines. The tiny silhouette on the rooftop resolves into the full sterile Tier-1 kit of a prone sniper behind a suppressed Mk22-pattern DMR.
- **0:04 → 0:05.5 (approach & smash):** camera flattens to near-horizontal, lens push continues to 85mm equivalent, framing tightens to a low three-quarter rear of `OVER` — we see the rear of the Ops-Core FAST, the GPNVG-18 flipped down, the ComTac boom mic, breath vapor curling past the receiver. The camera drives forward *into* the scope's ocular, the frame eclipses into black as the eyepiece iris fills the image.
- **0:05.5 → 0:06 (hard smash-cut into scope):** on the dark frame the circular thermal-scope vignette snaps open with its crisp black iris, the thin crosshair reticle and milliradian hashes materialize, and the palette hard-cuts from teal-green night to white-hot thermal. Breath-synced parallax wobble of ~0.5° takes over. **End pose matches shot 02's hero keyframe exactly:** circular scope vignette framed on `A1`'s façade at ~10x, two white-hot ground-floor silhouettes and one pacing upstairs figure resolved, cold rear of compound registering as dark shapes.

## Action Evolution
- **Compound `AC-1`:** sharp and centered at 0:00, then slides off the bottom-left of frame during the 0:02 pivot. From 0:02–0:04 it drops out of the primary framing entirely; only a faint warm dot (south-gate bulb) and a faint amber rectangle (2nd-floor NW window) remain visible as distant reference points behind the sniper at 0:04–0:05.5. At 0:06 `A1`'s façade reappears — this time as a thermal signature inside the scope reticle.
- **Element Alpha (west column, 5 operators):** tiny specks crossing the walled crop plot at 0:00, blur past the lower-left of frame during the dive, no longer visible by 0:03. Their pale-green per-operator labels detach from the helmets and retract toward the top-right command tree.
- **Element Bravo (east column, 4 operators):** single-file along the east irrigation ditch at 0:00, streaks across the lower-right during the pivot, gone by 0:03. Their labels retract in the same motion.
- **Sniper `OVER`:** the tiny silhouette at 0:00 grows frame-by-frame. At 0:02 he is a recognizable prone body on the outbuilding rooftop. At 0:04 we read individual kit: the Mk22-pattern DMR across a rolled shemagh on the parapet, no bipod deployed, no spotter, IR strobe pulsing on the back plate of the FAST helmet. By 0:05 his gloved trigger-finger indexed along the receiver and the clear ballistic eye-pro catch the last beat before the camera punches into the ocular.
- **Mesh threads (diegetic cyan BLE):** at 0:00 they span the whole frame; as the camera yaws and dives, non-`OVER` threads fade out in a cascade (furthest first — Alpha ↔ Bravo, then TL ↔ operator links), leaving only the single thread that links `OVER`'s node back toward `SL`. By 0:04 that one thread is the last connective tissue in the frame; by 0:05 it too fades as the frame eclipses into the ocular.
- **Environment:** walled crop plots, dry-stone wall, irrigation ditches, and the olive/poplar tree line all streak through the frame as parallax layers during the 0:02–0:04 dive. Ankle-height dust ribbons on hard-packed dirt spin horizontally under the lens. The distant terraced farmland and dusty mountain ridge compress into the background plate. No fog, no rain, no asphalt — the Style Bible's ground truth holds.
- **Palette shift:** teal-green shadows with warm amber highlights from 0:00 to ~0:05.5. On the smash-cut the outside world is replaced by the white-hot thermal palette inside the scope iris; the reticle ring itself carries a faint chromatic bloom that echoes the teal-green edge we just came from, as a one-frame transition tell.

## HUD Overlay Evolution
HUD behaves as a transparent diegetic holographic AR layer throughout — never a split-screen panel, never solid blocks. Color key inherits the Style Bible: pale-green `#7FFFA9` for raw `[SOLDIER → AI]`, dim amber `#FFB347` for `[AI ROUTING]` metadata, bright cyan-white `#EAFBFF` reserved for `[LEADER EARPIECE]` (not used until the far end of this bridge).

- **0:00 — inherited from 01's end pose:** ten pale-green per-operator labels pinned to helmets, element-level callouts `ELEMENT ALPHA · 5`, `ELEMENT BRAVO · 4`, and `OVER · SNIPER · 320 m` with long tick lines, dashed cyan mesh threads pulsing, dim amber lower-third status band "TACNET // MESH ONLINE  10/10 NODES  BLE · OFFLINE · ON-DEVICE SLM", bottom-left timestamp `03:02:14` → `03:02:20`, command-tree micrographic top-right with nodes pulsing pale green, TACNET lockup 40% bottom-right.
- **0:00 → 0:02 (fade-out cascade):** as the camera pivots and yaws, the element-level callouts and the status band fade out first (amber → 0%). The nine non-`OVER` per-operator labels retract along hair-thin amber trace lines toward the top-right command tree, where their nodes dim to ~30%. The `OVER · SNIPER · 320 m` tick line grows as the callout itself stays anchored to the sniper silhouette — it *shortens* as the camera closes the distance, reading like a range-finder collapsing toward zero.
- **0:02 → 0:04 (single-node focus):** only the `OVER` callout, the cyan mesh thread from `OVER` back toward `SL`, and the persistent furniture (command tree top-right, timestamp bottom-left incrementing to `03:02:24`, TACNET lockup bottom-right) remain. The `OVER · SNIPER · 320 m` numeral ticks down in sync with the dive — `280 m → 200 m → 120 m → 60 m` — as a pulsing pale-green readout. The command tree visibly reflows: `SL` and all child nodes compress toward the top-right corner while the `OVER` node enlarges and migrates toward the tree's center-stage position, its pale-green pulse intensifying.
- **0:04 → 0:05.5 (scope handoff prep):** the `OVER · SNIPER · [range]` label converts to `OVER · SCOPE ACTIVE` in pale green with a short tick line to the ocular of the DMR. The last surviving cyan mesh thread animates one soft packet pulse back toward `SL` — a visual promise of the line that is about to arrive in shot 02. Command tree now shows `OVER` center-stage with a pale-green ring; `SL` node glows faint amber in anticipation (not yet cyan-white — that beat is reserved for shot 02's `[LEADER EARPIECE]` moment).
- **0:05.5 → 0:06 (smash into scope):** HUD furniture persists through the eclipse. On the other side of the smash-cut the scope vignette resolves with: circular black iris, crosshair reticle, milliradian hashes, tiny pale-green `OVER · 320 m` tag tick-lined to the reticle center, command tree holding top-right with the `OVER` node still pulsing pale green, timestamp bottom-left now reading `03:02:28` (the opening stamp of shot 02), TACNET lockup holding bottom-right. No pale-green whisper text, no amber routing line, no cyan-white earpiece block yet — those are the beats shot 02 opens on. The HUD state at 0:06 matches shot 02's t = 0.0 s exactly.

## Audio Timeline
- **0:00:** tail of shot 01's tenth soft mono handshake chime, still ringing. Thin high-altitude drone wind, 1 Hz rumble from the drone sensor characterization. Distant faint crunch of Bravo's boots along the ditch.
- **0:00 → 0:02:** a single low sub-bass *whumph* on the pivot as the camera breaks its top-down lock — one kick of air pressure, no musical score. Drone wind thins; boot crunch fades under the motion.
- **0:02 → 0:04:** the rush of the dive — a soft whoosh of wind across the lens (not a movie-trailer riser, just air past a fast-moving camera), layered with a descending thin digital chirp as non-`OVER` HUD nodes dim and retract. A faint metallic tick as each per-operator label locks into the top-right command tree.
- **0:04 → 0:05.5:** wind thins almost to silence on the ridge — we are arriving in `OVER`'s environment. A single shallow breath from the sniper, mic-close. One pulse of the FAST helmet's IR strobe registers as a near-silent electrical tick. The last cyan mesh-packet chime plays as the `OVER → SL` thread carries its soft pulse.
- **0:05.5 → 0:06:** the smash-cut lands with a tight, dry *clack* — the diegetic sound of pressing an eye into a rubber ocular, not a cinematic bass drop. Immediately the high-ridge wind of shot 02 takes over, steady, no gusts. We arrive on silence ready for `OVER`'s first whispered line. No musical sting. No radio static burst.

## Beat Markers
- **0:00** — Pure top-down bird's-eye at ~40 m over compound `AC-1`, ten operators dispersed in choreography per shot 01's end pose. All HUD layers from 01 present: ten per-operator labels, element-level callouts, cyan BLE mesh threads, amber status band, command tree top-right pulsing pale green, timestamp `03:02:14–20`.
- **0:02** — Midpoint A: camera has pivoted from 90° overhead to ~60° oblique, yaw swung to face NNE, compound sliding off the lower-left of frame, the distant ridge outbuilding with `OVER` now screen-centered and growing, altitude still ~40 m. Element callouts and amber status band have faded. Only `OVER · SNIPER · 280 m`, the `OVER → SL` cyan thread, and the persistent corner furniture remain. Low sub-bass *whumph* tail.
- **0:04** — Midpoint B: camera is low and fast, ~8 m altitude, tilt near 30° down and flattening, 50mm push, landscape streaking past as motion-blurred parallax layers. `OVER` is clearly readable in sterile Tier-1 kit with his Mk22-pattern DMR on the parapet. Range readout pulses `120 m`. Command tree has visibly reflowed so `OVER` is center-stage, enlarged, pulsing pale-green; `SL` node glowing faint amber. Dive-wind under a descending thin digital chirp.
- **0:06** — End frame matches shot 02's opening pose exactly: circular thermal-scope vignette with crosshair reticle and milliradian hashes fills the frame, white-hot palette replaces teal-green, `A1`'s façade inside compound `AC-1` resolves with two ground-floor heat sigs and one pacing upstairs figure, rear of compound cold. HUD furniture: tiny pale-green `OVER · 320 m` tick-lined to reticle center, command tree top-right with `OVER` pulsing pale green, timestamp `03:02:28`, TACNET lockup bottom-right at 40% opacity. High-ridge wind only — silent and ready for the whisper.

## Video Generator Prompt (start-frame → end-frame)
> Photoreal cinematic 6-second bridging shot, 16:9 composed for a 2.39:1 anamorphic letterbox crop, 4K source with subtle 35mm film grain, gate weave off, no chromatic aberration except a faint chromatic bloom inside the final scope vignette, 0300 hours on a moonless overcast night with thin high clouds, very dark ambient, dry and cold ~8°C, ankle-height dust ribbons drifting and then streaking across hard-packed dirt — no fog, no rain, no asphalt. **Start frame = shot 01 keyframe (dispersed-raid bird's-eye at low altitude above compound `AC-1`); end frame = shot 02 keyframe (thermal-scope POV through `OVER`'s Mk22-pattern DMR at ~10x).** The camera begins in a pure top-down dead-vertical 90° overhead drone gimbal at roughly 40 m altitude with the full fortified walled compound `AC-1` — one-acre irregular rectangular footprint, 14–18 ft weathered concrete perimeter walls topped with rusted razor-wire coils, wide south steel vehicle gate beneath a single bare orange incandescent bulb, narrow west pedestrian gate, three-story concrete-and-stucco main residence `A1` in the north half with a dim amber-lit 2nd-floor northwest window, single-story cinderblock annex `A2` in the southwest corner, battered white Toyota Hilux, diesel generator shed, chicken coop, vegetable patch, surrounding walled crop plots, irrigation ditches, olive/poplar tree line, distant terraced farmland and dusty mountain ridge — locked in frame, ten dispersed Tier-1 operators readable as tiny choreographed figures (Element Alpha of five in a loose west column through the crop plot toward the west pedestrian gate, Element Bravo of four single-file along the east irrigation ditch, sniper callsign `OVER` prone on the flat roof of a small isolated farm outbuilding roughly 320 meters north-northeast on elevated ridge terrain with a suppressed Mk22-pattern DMR resting across a rolled shemagh on the parapet). Over the first two seconds the camera breaks its top-down lock with a gentle Dutch de-roll to 0°, then yaws clockwise to face north-northeast while tilting forward from 90° overhead toward 60° oblique and keeping altitude near 40 m so the compound slides out of the lower-left of the frame and the distant ridge outbuilding with `OVER` swings to screen center and begins growing. Over the next two seconds the camera accelerates into a low fast dolly across the landscape toward `OVER`'s hide, altitude dropping from 40 m to 8 m, tilt flattening from 30° down toward horizontal, lens pushing from 28mm through 35mm to 50mm equivalent, the walled crop plots, dry-stone wall, irrigation ditches, and ragged olive/poplar tree line streaking past as motion-blurred parallax layers with ankle-height dust ribbons spinning horizontally through the foreground. In the final second and a half the camera flattens to near-horizontal on a low three-quarter rear of the prone sniper in sterile Tier-1 kit (Crye G3 multicam-black, Ops-Core FAST SF helmet with IR strobe pulsing on the back plate, L3Harris GPNVG-18 flipped down, Peltor ComTac boom mic, clear ballistic eye-pro, Crye JPC 2.0 plate carrier with no patches or flags, Safariland drop-leg Glock 19, tan Mechanix gloves, Salomon Forces boots, suppressed Geissele Super Duty MK228 / Mk22 MRAD DMR variant on a rolled shemagh across the low concrete parapet, thin breath vapor curling past the receiver, no bipod, no spotter), pushes to 85mm and drives forward *into* the scope's ocular so the frame eclipses into black, then hard smash-cuts on the final half-second into the circular thermal-scope POV that opens shot 02: crisp black iris, thin black crosshair reticle with milliradian hashes centered, faint chromatic bloom at the scope edge, breath-synced ~0.5° parallax wobble, white-hot thermal palette replacing the teal-green night palette, two white-hot human silhouettes standing close in the ground-floor front living room of `A1` holding long-gun heat sigs, one white-hot figure pacing the 2nd-floor northwest bedroom past the curtained amber window, the rear of the compound registering completely cold, and the end pose matching the shot 02 keyframe exactly. Throughout the bridge the palette outside the scope carries teal-green shadows and warm amber highlights only on the single 2nd-floor northwest window and the south gate bulb, crushed blacks with a faint green lift, anamorphic horizontal flares on IR strobes and HUD glyph blooms, midtones desaturated twenty percent; inside the scope at the end the palette is white-hot thermal. Layered DIRECTLY ON TOP OF THE IMAGE as a transparent holographic AR overlay (NOT a split-screen panel, NOT a solid block, NOT picture-in-picture) in thin monospace all-caps typography with subtle CRT scanlines, micro glitch every two seconds, Gaussian bloom, and only three-to-five percent darkened backing behind glyph footprints, the HUD evolves across the 6 seconds: at the start ten pale-green `#7FFFA9` per-operator labels pinned to each helmet, element-level callouts `ELEMENT ALPHA · 5` and `ELEMENT BRAVO · 4` and `OVER · SNIPER · 320 m`, dashed cyan BLE mesh threads spanning the frame, a dim amber `#FFB347` lower-third status band reading `TACNET // MESH ONLINE  10/10 NODES  BLE · OFFLINE · ON-DEVICE SLM`, a 10-node command-tree micrographic top-right with nodes pulsing pale-green, a bottom-left mono timestamp beginning at `03:02:20` and ticking to `03:02:28`, and a 40% opacity `TACNET` wordmark lockup bottom-right; as the camera pivots and dives, the amber status band and the nine non-`OVER` per-operator labels fade out first and retract along hair-thin amber trace lines toward the top-right command tree, the `OVER · SNIPER · 320 m` range numeral ticks down `280 → 200 → 120 → 60` in pale green, the command tree visibly reflows so `SL` and child nodes compress into the corner while the `OVER` node enlarges and migrates to center-stage with an intensifying pale-green pulse, and the single surviving cyan mesh thread between `OVER` and `SL` carries one soft packet pulse; on the smash-cut into the scope the HUD state resolves to shot 02's opening furniture — tiny pale-green `OVER · 320 m` tag tick-lined to the reticle center, command tree holding top-right with `OVER` pulsing pale green, timestamp `03:02:28`, `TACNET` lockup 40% bottom-right — with no pale-green whisper text, no amber routing line, and no cyan-white earpiece block yet (those beats belong to shot 02). HUD glyphs never cover any operator body, weapon, helmet, the three heat signatures, the reticle cross, the amber 2nd-floor NW window, or either compound gate — all text lives in negative space over empty fields, rooftops, sky, parapet concrete, and the black void of the night horizon. Hyper-realistic, cinematic, disciplined, silent pre-contact tension, a single continuous scale-jump dive from god-view of a dispersed Tier-1 raid to a lone sniper's optic 320 meters out.
